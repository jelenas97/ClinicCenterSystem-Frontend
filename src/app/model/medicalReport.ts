import {User} from './user';
import {Diagnosis} from './diagnosis';

export class MedicalReport {
  report: string;
  therapy: string;
  doctorId: string;
  diagnosisId: string;
  medicamentId: string;
}
